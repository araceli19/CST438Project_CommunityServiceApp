machine:
  php:
    version: 5.6.24
test:
  override:
     - vendor/bin/phpunit --configuration configuration/test.xml

  post:
    - bash <(curl -s https://codecov.io/bash)
        notify:
	  webhooks:
	   - url: http://csumbse.net/harbormaster/hook/circleci/
